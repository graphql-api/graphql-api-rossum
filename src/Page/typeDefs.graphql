# import Node from '../Node.graphql'

type Page {
  id: Int!
  url: String
  annotation: String
  number: Int
  rotation_deg: Int
  mime_type: String
  s3_name: String
  content: URL
  metadata: JSON
}

type Query {
  listPages: [Page]
  retrievePage: Page
}
